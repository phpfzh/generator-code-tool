package com.${company!''}.${project!''}.<#if module??>${module}.</#if>mapper;

import com.${company!''}.${project!''}.<#if module??>${module}.</#if>model.${model};
import com.ganjiangps.huochetou.SuperMapper;

public interface ${model}Mapper extends SuperMapper<${model}> {
}
